# Передача контекста: Заявка → Калькуляция → Спецификация → Продукт

Дата создания: 2026-08-15. Полностью переписан (сжат) 2026-08-19 — старая
версия про комплекты разрослась и стала мешать читать актуальное; история
комплектов свёрнута в раздел 0, детали при необходимости смотреть в git-
истории этого файла.

Это НЕ замена docs/HANDOFF.md — отдельный документ по конкретной теме
(документооборот заявка/калькуляция), которая спроектирована на уровне
решений, но код ещё не писался (кроме комплектов — см. раздел 0).

**Инструкция для новой сессии:** прочитать этот файл целиком (он теперь
короткий), затем docs/HANDOFF.md для текущего состояния кода/версии.
`client`+`request` (бывшая `zayavka`) уже закодированы (v49, раздел 2.2/2.2.1) —
начинать кодирование с `calculation` (раздел 2.3).

---

## 0. Комплекты (kit) — ЗАВЕРШЕНО, код в проде

`kit_group → kit_section → kit → kit_items`, включая полноэкранный
MaterialPicker для редактирования состава. Полностью реализовано и
задеплоено, актуальная версия — см. docs/HANDOFF.md. Здесь не описывается
детально — это уже не проектирование, а готовый код.

Единственный принцип оттуда, который важен для калькуляции: калькуляция
хранит ССЫЛКУ на kit (`kit_id`), не снимок состава — при пересчёте состав
комплекта разворачивается заново на текущий момент ("live", не snapshot).
Комплекты можно только soft-delete (не физически) из-за возможных ссылок
на них из калькуляций.

---

## 1. Общий контекст проекта

ЭлектроЩит — веб-приложение на FastAPI + SQLModel + Alpine.js + Jinja2 +
PostgreSQL (Railway), заменяет 1С для расчёта и учёта электрощитов.
Репозиторий: https://github.com/Vah509/Calc_el_panel.git
Прод: https://calcelpanel-production.up.railway.app
Разработка ведётся с телефона: архив → ai-updates/ на GitHub → Actions
staging → promote → Railway автодеплой.

Есть универсальный CRUD-движок (`app/engine/`) с TableConfig/FieldConfig,
soft-delete, processors, пагинацией и т.д. Детали инфраструктуры и все
наработанные принципы проекта — в основной памяти Claude (userMemories),
этот файл её не дублирует.

Принцип работы: строго пошагово, не забегать вперёд, каждый шаг — отдельный
архив с версией. Названия таблиц/полей всегда согласовывать с Вахтангом до
фиксации в коде.

---

## 2. Модель документооборота (спроектировано, код НЕ писался)

Цепочка: **zayavka → calculation → specification → invoice → (product)**

### 2.1 Порядок реализации (решено, не менять без причины)
1. ~~`client` (справочник) + `request` (заявка)~~ — ГОТОВО, код в проде
   (v49, раскладка карточки поправлена в v50). Простой CRUD-документ,
   кнопки формирования спецификации и пересчёта — видимые неактивные
   заглушки, по паре на каждый брендовый слот (`FieldConfig.row_actions`).
   Общей кнопки "Пересчитать всё" нет — сознательно убрана в v50.
2. `calculation` — следующий шаг. С реальным FK `request_id` → request
   (не заглушка, request уже существует); попутно, при сохранении
   calculation — создание/обновление записи в `product` (см. 2.5)
3. `calculation_item` — материалы/комплекты внутри калькуляции, движок
   пересчёта
4. `specification` + `specification_item` — снимок по варианту, кнопки
   формирования в request получают реальную логику вместо заглушек

### 2.2 Таблица `client` (справочник клиентов) — ЗАКОДИРОВАНО (v49)
Не существовало в проекте до этого шага — создана с нуля вместе с request
(закрывает открытый вопрос из более раннего наброска этого файла).
Поля (финальные, как в коде `app/models/client.py`):
- `short_name` — короткое название, внутреннее, для удобного поиска
- `full_name` — полное название, фигурирует во всех документах (счетах,
  спецификациях)
- `egrpou_code` — код ЕГРПОУ
- `phone` — общий телефон клиента
- `contact_person_name`, `contact_person_phone` — контактное лицо
  (отдельный телефон, не совпадает с общим телефоном клиента)
- `is_deleted` — стандартный soft-delete проекта

### 2.2.1 Таблица `request` (заявка) — ЗАКОДИРОВАНО (v49-v51)
Поля (финальные, как в коде `app/models/request.py`; отличие от более
раннего наброска этого раздела — ниже разбираются построчно):
- `document_number` (str, редактируемый) и `document_date` (date,
  редактируемый) — добавлены в v51, не было в раннем наброске вообще.
  Номер автогенерируется при создании по счётчику префикса "R", если не
  введён вручную; дубликаты не проверяются; ручная правка номера на
  бо́льшее число подтягивает счётчик вверх. Дата при создании — сегодня
  по умолчанию, дальше свободно редактируется. Общая механика (не только
  для request) — `app/engine/document_numbering.py` +
  `app/models/document_counter.py`, см. HANDOFF.md v51 для полной истории
  решения (два счётчика: уникальный id записи vs редактируемый видимый
  номер — обсуждали именно так, стандартный паттерн, отдельная таблица
  DocumentCounter по одной строке на префикс).
- `client_id` (FK → client, обязателен) — кто оформляет заявку
- `client_invoice_id` (FK → client, nullable) — на кого выставляется счёт,
  если отличается от оформителя. **Уточнение к более раннему наброску:**
  "по умолчанию = client_id" — это правило будущего UI/логики, НЕ constraint
  модели; в БД оба поля независимы, дефолт (если понадобится) будет решаться
  на уровне API/фронта при появлении реальной логики формирования счёта.
- `brand_slot_1_id`, `brand_slot_2_id`, `brand_slot_3_id` (FK → brand,
  каждый nullable) — **закрыт открытый вопрос** из раннего наброска: это
  FK на Brand, не текст. Все три слота ВСЕГДА видны в форме (не динамический
  список), бренд может быть не выбран.
- `is_deleted` — стандартный soft-delete проекта
- Копирование — РЕШЕНО иначе, чем в раннем наброске: не server-side copy
  (тот эндпоинт в движке ограничен таблицами с `edit_mode="items_modal"`,
  у request его нет) — работает штатный client-side copy движка, доступный
  любой не-hierarchy таблице через `isSelectableLevel()`. Разницы для
  пользователя нет, реализация проще.
- Физическое удаление — отдельным обработчиком (purge), работает "из
  коробки" для любой `delete_mode="soft"` таблицы — ничего специального
  для request писать не пришлось.
- Кнопки "Спецификация" и "Пересчитать" — по паре на каждый брендовый
  слот (не общий блок под формой). **История решения:** в v49 сначала
  сделали 4 общие кнопки под формой (3× "Спецификация — вариант N" +
  общая "Пересчитать всё"), рядом с полями бренда никак не привязанные.
  По фидбеку с реального устройства (скриншот, 2026-08-19) переделано в
  v50: у каждого слота своя строка (select на всю ширину) с кнопками
  "Спецификация"/"Пересчитать" СПРАВА от него в том же ряду
  (`FieldConfig.row_actions`) — понятно, какая кнопка к какому варианту
  относится. Общая кнопка "Пересчитать всё" в v50 УБРАНА совсем — решение
  Вахтанга: пересчитывать каждый вариант отдельно проще для понимания,
  чем городить логику "пересчитать всё и понять что где обновилось".
  Кнопки задизейблены, видны только у существующей записи. Реальная
  логика (актуализация цен + полная перезапись specification, как
  раньше и планировалось: delete-all+create-all, как kit_items) —
  реализуется вместе с calculation/specification, НЕ в этом шаге.
- Дата/признак "когда обновлялись цены" — РЕШЕНО НЕ хранить (излишнее
  усложнение по прямому решению Вахтанга)
- Статус — РЕШЕНО НЕ нужен (подтверждено 2026-08-19). Вместо статуса —
  три независимые операции: копирование, пометка на удаление (+ физическое
  удаление обработчиком), архивация. Архивация — отдельный будущий
  обработчик, НЕ реализована в v49, концепции ещё нет нигде в коде.

### 2.3 Таблица `calculation` (калькуляция)
Одно изделие — один вариант (один brand_slot) в рамках одной заявки.
- `request_id` — реальный FK на zayavka
- `brand_slot` (1/2/3) — группирующий признак вариантов внутри заявки
  (совпадает по смыслу с `brand_slot_N` в zayavka)
- `brand_id` (FK на бренд) — справочное поле, НЕ группирующий признак
  (группировка именно через номер слота)
- `product_name` — название изделия, вводит человек, свободный текст
- `work_name` — рабочее название, генерируется по шаблону (см. 2.4)
- `quantity` — количество этого изделия в заявке
- `status` — draft/active/archived_pending/delete_pending, точная механика
  переходов НЕ проговорена (открытый вопрос)
- `product_id` — FK на `product` (см. 2.5), один-к-одному
- строки калькуляции — `calculation_item` (материал ИЛИ kit × количество,
  см. 2.3.1). Повтор одного и того же kit в разных строках — норма, не
  дублирование-ошибка

**Логика "перепровести" (пересчитать калькуляцию):**
1) актуализируются цены материалов, 2) live-разворачивается состав
комплектов на текущий момент, 3) собирается итоговая спецификация
материалов и итоговая цена изделия. Если состав kit изменился с прошлого
пересчёта — лог/предупреждение, не молчаливая потеря/добавление строки.

#### 2.3.1 `calculation_item` (строки калькуляции) — ОТКРЫТО, детали не расписаны
Модель верхнего уровня подтверждена: одна таблица, два nullable FK
(`material_id` / `kit_id`), тип строки — по тому, какое поле заполнено.
На фронте — две вкладки "Материалы"/"Комплекты" (чисто UI-разделение, не
влияет на структуру таблицы). Точные остальные поля — решить при
кодировании этого шага.

### 2.4 Правило генерации `work_name`
- Текущее правило: `"Сборка " + product_name`
- Хранится НЕ жёстко в коде, а как настраиваемый текстовый шаблон в
  constants-таблице — чтобы в будущем добавить номер заявки/код бренда без
  правки кода
- Автоген с возможностью РУЧНОГО переопределения: если человек вручную
  поправил work_name — флаг "переопределено", авторасчёт больше не
  перезаписывает поле, пока флаг не сброшен

### 2.5 Таблица `product` (журнал изделий для поиска)
Назначение: чтобы через долгое время можно было найти изделие по названию
+ примерному клиенту, не разбираясь, на какой стадии документооборота оно
находится. Плоский справочник по тому же принципу, что `material` —
обычная таблица движка с текстовым поиском по полям.

**Механика связи с calculation (один-к-одному, calculation → product):**
- При каждом сохранении `calculation` — создаётся (если нет) или
  обновляется (если есть) соответствующая запись `product`. Направление:
  calculation ТОЛКАЕТ обновления в product — не наоборот, product не тянет
  live-данные при обращении (в отличие от specification/kit)
- `calculation.product_id` — FK хранится на стороне calculation
- Запись product живёт, пока жива сама calculation
- **ОТКРЫТО:** что происходит с product при удалении calculation — прямо
  отложено Вахтангом ("отдельная процедура, подумаем отдельно"). Не
  реализовывать удаление calculation, пока это не обсуждено
- Запись может никогда не понадобиться для поиска — это ок, создаётся как
  задел на будущее, не по требованию

**Примерные поля (не финализированы):**
`name`/`work_name`, `client_name`, `calculation_id` (обратная ссылка для
провала в историю расчёта), `price` (на момент последнего сохранения
calculation), возможно `status`.

**Важно:** это заменяет собой более раннее представление о `finished_products`
как отдельной таблице только для физически готовых/отгруженных изделий.
Вместо этого — единая таблица `product`, живущая с первого сохранения
calculation. Открыт вопрос: нужно ли отдельное поле-статус "физически
готово/отгружено" внутри той же product, или это будет отдельная сущность
поверх product — решить ближе к работе над invoice.

Реализация `product` — НЕ отдельный шаг, подключается как побочный эффект
при кодировании calculation (шаг 2 в разделе 2.1).

### 2.6 Таблица `specification` + `specification_item`
Промежуточный документ между calculation и invoice. Назначение: перечень
изделий одного варианта не всегда переносится в счета 1-в-1 — из одной
спецификации может быть создано НЕСКОЛЬКО счетов-фактур (например, из 5
изделий сначала счёт на 3, потом на 2). Спецификация даёт стабильную базу
для такой гибкой нарезки.

Из-за этого — ЭТО СНИМОК, не live-ссылка (в отличие от kit_items).
Обоснование: если бы цена была live и калькуляцию пересчитали после того
как часть спецификации уже нарезали на выставленный счёт — счёт задним
числом "поехал" бы в цене, что недопустимо для финансового документа.

**`specification`:** `request_id` (FK), `brand_slot` (1/2/3)

**`specification_item`:**
- `specification_id` (FK)
- `calculation_id` (FK) — НЕ для live-подтяжки цены, а чтобы при повторном
  формировании пройти по ссылкам и перечитать актуальные значения
- `product_name`, `price`, `quantity` — скопированы на момент формирования

Механика формирования (кнопка "Спецификация" рядом с брендовым слотом N в
request — с v50 заглушка `FieldConfig.row_actions`, логика ниже
реализуется вместе с этим документом): пересчитать все calculation
этого (request_id, brand_slot) → для каждой взять актуальные name/price →
полностью переписать
specification_item (delete-all+create-all). Между нажатиями кнопки данные
не меняются сами — чистый статичный снимок.

**ОТКРЫТО:** счёт-фактура при создании из спецификации — тоже копирует
себе данные (снимок снимка), или может сослаться на specification_item
напрямую? Вероятно тоже снимок по той же логике, но не подтверждено —
уточнить в начале работы над invoice.

---

## 3. Что осталось открытым (собрано в одном месте)

- **Статус calculation** — набор значений известен (draft/active/
  archived_pending/delete_pending), но точная механика переходов между
  ними не проговорена
- **calculation_item** — точные поля не расписаны, только модель верхнего
  уровня (два nullable FK)
- **Удаление calculation** — что делать с привязанной записью product,
  сознательно отложено
- **Invoice (счёт-фактура)** — не спроектирован вообще, будет актуален
  после specification. В т.ч. вопрос "снимок ли invoice_item" (см. 2.6)
- **Статус "физически готово/отгружено"** — быть ли отдельным полем в
  product или отдельной сущностью поверх него — решить ближе к invoice
- **Архивация request** — подтверждена как нужная (2026-08-19), но
  механика не спроектирована: отдельный будущий обработчик, простое поле
  типа `is_archived` пока считается достаточным, без привязки к статусу
  calculation (у calculation свой независимый статус)

Закрыто в v49 (для истории, не поднимать заново): brand_slot_1/2/3 в
request — это FK на Brand, не текст; таблица Client не существовала —
создана с нуля вместе с request.
