# app/main.py
# ============================================================
# ЭТО НЕ РАБОЧЕЕ ПРИЛОЖЕНИЕ, А ПРОБНИК.
#
# Единственная цель этого файла — дать test_build.py что-то
# реальное, что можно поднять через uvicorn и опросить на /health,
# чтобы ты своими глазами увидел, как автозагрузчик отрабатывает
# весь цикл: staging → тестовая сборка → перенос в репозиторий →
# сообщения в Telegram → удаление ZIP.
#
# Когда убедишься, что цикл работает — просто заливаешь архив с
# настоящим приложением (шаг 1) поверх этого пробника, тем же
# способом, через ai-updates/. Он полностью его заменит.
# ============================================================

from fastapi import FastAPI

app = FastAPI(title="Smoke Test — проверка автозагрузчика")


@app.get("/health")
def health():
    return {"status": "ok", "note": "Это тестовый пробник, не настоящее приложение"}


@app.get("/")
def root():
    return {"message": "Автозагрузчик работает! Теперь можно заливать настоящий проект."}
