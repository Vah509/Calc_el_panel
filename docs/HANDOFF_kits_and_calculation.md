# Передача контекста: Комплекты + Калькуляция изделия

Дата создания: 2026-08-15. Обновлено: 2026-08-19.

Статус: **Комплекты (kit_group → kit_section → kit → kit_items, включая
MaterialPicker) полностью реализованы, актуальная версия v42.** Разделы 2.2,
2.7 и связанные с ними открытые вопросы (3.4, 3.5, 3.9) — история, оставлены
для справки, НЕ актуальная работа.

**Текущий фокус: раздел 5 (добавлен 2026-08-19) — модель заявка →
калькуляция → спецификация, спроектирована на уровне решений, код ещё
не писался. Начинать новую сессию по этой теме — с раздела 5, а не с начала
файла.**

Это НЕ замена docs/HANDOFF.md — это отдельный документ по конкретной теме
(комплекты + калькуляция).

---

## 1. Общий контекст проекта

ЭлектроЩит — веб-приложение на FastAPI + SQLModel + Alpine.js + Jinja2 +
PostgreSQL (Railway), заменяет 1С для расчёта и учёта электрощитов.
Репозиторий: https://github.com/Vah509/Calc_el_panel.git
Прод: https://calcelpanel-production.up.railway.app
Разработка ведётся с телефона через чат с Claude: архив → ai-updates/ на
GitHub → Actions staging → promote → Railway автодеплой.

Текущая версия кода: v34. Есть универсальный CRUD-движок (app/engine/) с
TableConfig/FieldConfig, soft-delete, processors, пагинацией и т.д.
Все детали инфраструктуры, известные баги и наработанные принципы — в
основной памяти Claude по этому проекту (userMemories), этот файл её не
дублирует, а фокусируется на теме комплектов/калькуляции.

Принцип работы: строго пошагово, не забегать вперёд, каждый шаг — отдельный
архив с версией. Названия таблиц/полей всегда согласовывать с Вахтангом
до фиксации в коде.

---

## 2. К чему пришли по обсуждению (ЗАФИКСИРОВАНО, не переоткрывать без причины)

### 2.1 Порядок работы над темой
Выбран путь: спроектировать верхнеуровневую цепочку документов
(заявка → калькуляция → спецификация → счёт) на уровне общей формы связей
СЕЙЧАС, до кода, а кодировать — снизу вверх, начиная с того, что не имеет
зависимостей. Первый практический шаг в коде — kit_section.

Причина: калькуляция изделия как документ ещё не существует вообще, и без
понимания её формы есть риск спроектировать kit/kit_items неудобно для
будущей калькуляции и потом переделывать.

### 2.2 Комплекты (kit) — архитектура отображения и структуры
- `kit_section` — справочник разделов комплектов. Обычная таблица через
  universal CRUD engine. Нужен sort_order для управляемого порядка обхода
  (у Вахтанга в 1С была практика: открыл раздел → выбрал нужные комплекты →
  закрыл → следующий раздел, порядок важен для удобства работы).
  Примеры разделов (не финальные, для ориентира): подключение силовых
  автоматов, подключение модульных автоматов, установка силовых автоматов,
  установка модульных автоматов, расширительные шины и т.д. Точный список
  разделов ещё нужно проговорить отдельно.
- `kit` — карточка комплекта. НЕ обычная списковая таблица движка, а
  отдельная страница-карточка. Поля: название, ссылка на раздел (kit_section).
  Soft-delete как везде в проекте.
- `kit_items` — состав комплекта: вложенный редактируемый список внутри
  карточки kit. Каждая позиция — материал + количество. Позиции можно
  удалять физически сразу, БЕЗ soft-delete (обоснование см. 2.3).
  Операции: изменить количество, скопировать позицию, добавить позицию.
- Отображение — трёхуровневая раскрывающаяся структура:
  раздел (свёрнут по умолчанию) → комплекты внутри раздела (клик разворачивает)
  → состав комплекта (клик разворачивает, редактирование внутри).
  Это кастомный UI-компонент, не стандартная таблица движка.
- Для самого kit (не kit_items) нужны те же операции, что и у обычных
  строк движка: копирование, создание нового. Удаление — только soft-delete
  (пометка), т.к. в комплектации могут быть ссылки на него из калькуляций.
  Нужен отдельный processor (по аналогии с существующим purge processor),
  который перед физическим удалением комплекта проверяет, не используется
  ли он где-то (в калькуляциях).

### 2.3 Принцип "live-разворачивание" комплекта в калькуляции
Решено: калькуляция хранит ССЫЛКУ на kit (kit_id), а НЕ снимок его состава.
При действии "перепровести" система заново идёt в текущий состав kit_items
и разворачивает его на момент пересчёта. Это следует общему принципу проекта
"live references vs snapshots" (снимки — только для фактов расчёта типа
price_snapshot).
Следствие: позиции материала внутри комплекта можно удалять сразу, без
soft-delete — потому что калькуляции не хранят копию состава, только ссылку
на kit. Если состав комплекта изменился, а комплект уже использовался в
старой калькуляции — при "перепровести" нужно показать предупреждение/лог,
что состав изменился, а не молча потерять/добавить строку.

### 2.4 Черновая модель калькуляции (верхний уровень, спроектировано СЕЙЧАС, код ещё не писался)

Цепочка документов: **заявка → калькуляция → спецификация → счёт**

**request (заявка)** — журнал заявок. Пока НЕ реализована в коде.
На время разработки калькуляции без готовой таблицы request: либо временно
nullable FK, либо одна техническая заявка-заглушка, к которой всё привязано,
пока настоящих заявок нет. Архитектурно связь request→calculation
ОБЯЗАТЕЛЬНА (в проде это ошибка, если её нет), это временный обход только
для этапа разработки.

**calculation (калькуляция)** — одно изделие (один вариант, один бренд).
Поля (черновые, английские названия НЕ согласованы, только для ориентира):
- `request_id` (FK на заявку; временно nullable/заглушка — см. выше)
- `brand_id` (FK на бренд) — это РЕАЛЬНЫЙ группирующий признак вариантов
  одного изделия внутри заявки. НЕ название — важно, см. 2.5.
- `client_name` — название изделия как ввёл человек, свободный текст,
  БЕЗ технической роли, ненадёжно для любых связей (может быть опечатка).
- `work_name` — рабочее/внутреннее название для отображения в базе.
  Генерируется по правилу (какому именно — ОТКРЫТЫЙ ВОПРОС, см. раздел 3).
  Пример прикидки от Вахтанга: перед клиентским названием добавляется
  префикс "Сборка" (например клиент написал "ШМУ" → в базе "Сборка ШМУ").
  Возможно также нужен номер заявки и номер/код бренда в составе рабочего
  названия — это тоже не решено окончательно.
- `quantity` — количество этого изделия в рамках заявки (например, в заявке
  нужно 20 шт этого щита — это не 20 строк, а одна calculation с quantity=20).
- статус — НЕ до конца спроектирован, см. раздел 3.
- строки калькуляции (calculation_item, ещё не спроектированы детально) —
  либо материал напрямую, либо комплект (kit_id) × количество. Повтор одного
  и того же комплекта в одной калькуляции несколькими строками — это НОРМА,
  не дублирование-ошибка (Вахтанг специально подтвердил: может быть комплект
  №1 три раза, потом комплект №2 два раза, потом снова комплект №1 один раз —
  всё это потом схлопывается в единую спецификацию материалов позже).

**Логика "перепровести" (пересчитать калькуляцию):**
Пересчёт идёт по дереву: 1) актуализируются цены материалов по текущим
правилам, 2) разворачивается состав комплектов live (на текущий момент),
3) собирается итоговая спецификация материалов и итоговая цена изделия.
Если что-то в составе комплекта изменилось с прошлого пересчёта — лог/
предупреждение, не молчаливое исчезновение.

### 2.5 Почему группировка вариантов — через бренд, а не через название
Изначально Claude предположил, что три варианта одного изделия на разных
брендах связаны одинаковым client_name. Вахтанг поправил: НЕТ, названия
могут и должны отличаться, привязываться к названию нельзя (хрупко,
опечатка ломает связь). Реальная группировка вариантов одного изделия
внутри заявки — через `brand_id`. "Показать все изделия этого варианта
заявки" = "все calculation с этим request_id и этим brand_id". Отдельной
сущности "вариант/позиция изделия" не создаём — это просто срез по
(request_id, brand_id).

### 2.6 Операции над калькуляцией (начато, не закончено)
Кроме "перепровести", калькуляцию можно:
- копировать в ДРУГУЮ заявку — при копировании бренд НЕ переносится
  автоматически, его нужно вручную выбрать заново из списка брендов,
  доступных в целевой заявке (см. открытый вопрос 3.4 — есть ли у заявки
  свой ограниченный список допустимых брендов, или выбор из общего списка
  брендов системы).

### 2.7 UI-архитектура дерева (ЗАФИКСИРОВАНО в сессии 2026-08-16)

Обсуждали: kit_group → kit_section → kit — это не плоский список с фильтром,
а раскрывающаяся лесенка (tree). У Вахтанга в голове чёткая картина: клик на
группу — разворачивает список подгрупп (сдвинуты вправо), клик на подгруппу —
разворачивает список комплектов внутри неё, кнопка "новый комплект" тут же.
Один раскрытый узел на уровень (не аккордеон на весь экран, а именно "с чем
сейчас работаю — то и открыто").

Также добавлен уровень **kit_group** (над kit_section) — то есть иерархия
теперь ТРЁХуровневая: kit_group → kit_section → kit. Ранее в этом документе
(раздел 2.2) была двухуровневая версия (kit_section → kit) — это раздел 2.2
считать устаревшим в части числа уровней, актуально в остальном (soft-delete,
sort_order, purge-processor).

**Решение по универсальности компонента:**
НЕ делать полноценный универсальный tree-движок с абстрактным числом уровней
и конфигурируемым бэкендом сейчас. Причины: всего 2-3 конкретных случая
использования (kit-иерархия 3 уровня; ТМЦ group→subgroup 2 уровня; дерево
документов заявка→калькуляция — вообще другая природа, не справочник), и у
каждого своя специфика поведения (у kit нужны кнопки CRUD прямо в узле дерева,
у других случаев неизвестно заранее). Преждевременная абстракция дороже, чем
консервативный путь.

**Вместо этого — компромисс:**
- Фронтенд: один переиспользуемый Alpine.js компонент **TreeList** — рендерит
  N уровней вложенности из плоского массива узлов с `parent_id`, разворачивает
  один узел на уровень за раз, поддерживает конфигурируемые кнопки действий на
  каждом узле (создать/переименовать/пометить). Этот компонент переиспользуем
  для ТМЦ group→subgroup и других будущих иерархий БЕЗ переписывания с нуля.
- Бэкенд: НЕ единый абстрактный API. Для kit-иерархии — свои простые
  CRUD-эндпоинты (можно через universal CRUD engine как обычные справочники
  для kit_group/kit_section, либо отдельные простые эндпоинты — решить при
  кодировании; сейчас важно, что они возвращают данные в формате, понятном
  TreeList: плоский список узлов с id/parent_id/name/sort_order).
- Управление узлами (создание/переименование/soft-delete группы и подраздела)
  — ПРЯМО В ДЕРЕВЕ, кнопками у узла (+/✎/пометить), а не в отдельном списке
  движка. Это отличается от раздела 2.2, где предполагалось "обычный
  справочник движка" — теперь управление интегрировано в сам древовидный
  экран для удобства работы (открыл группу → тут же добавил подраздел →
  тут же добавил комплект, не переключаясь между экранами).
- Сам kit — нижний узел дерева. Клик по строке kit ведёт на ОТДЕЛЬНУЮ
  страницу-карточку (не разворачивается на месте внутри дерева). На
  странице-карточке — стандартные поля карточки + вложенная таблица
  kit_items с подбором материалов.
- Удаление узлов (kit_group, kit_section, kit) — НЕ мгновенное, через
  soft-delete + отдельный processor с проверкой использования (см. 2.2/2.3).
  ИСКЛЮЧЕНИЕ: позиции kit_items (материал+количество внутри карточки
  комплекта) удаляются/изменяются мгновенно без processor — так как это не
  справочник, а состав, и калькуляции хранят ссылку на kit, а не снимок
  состава (см. 2.3).

**Отдельно зафиксировано: MaterialPicker.**
Подбор материалов в kit_items потребует UI-компонент поиска/выбора материала
из справочника (с ценой, остатком и т.д.) для добавления строки в состав.
Тот же самый компонент понадобится позже для calculation_item (строки
калькуляции — материал напрямую). Решено спроектировать и сделать его один
раз как отдельный переиспользуемый компонент, качественно, а не дублировать
логику подбора материала в двух местах. Точный дизайн MaterialPicker —
отдельная задача, не начата.

**Порядок кодирования (обновлён):**
1. TreeList — фронтенд-компонент (без реальных данных, на моках/простом тесте)
2. kit_group + kit_section — бэкенд (простой CRUD, формат под TreeList) +
   подключение к TreeList на реальных данных (пока без kit)
3. kit — карточка (без kit_items, просто создание/базовые поля) + встраивание
   как нижний узел дерева в TreeList
4. MaterialPicker — отдельный компонент подбора материала
5. kit_items — состав комплекта на карточке kit, с использованием MaterialPicker
6. Возврат к архитектуре calculation_item — уже с готовым kit и MaterialPicker

**ВАЖНО — устарело по факту кода (см. docs/HANDOFF.md, v40):** порядок и
архитектура выше писались ДО того, как в коде kit стал открываться модальным
окном поверх дерева (`edit_mode="items_modal""), а не отдельной страницей-
карточкой (пункт 3 выше). TreeList на практике не понадобился как отдельный
универсальный фронтенд-компонент — реализован через `LEVELS[]`/`drillPath` в
общем `enginePage()`. Этот раздел (2.7) сохранён для истории решений и для
MaterialPicker/полосы ниже — при работе с деревом ориентироваться на
docs/HANDOFF.md, не на порядок кодирования здесь.

### 2.8 MaterialPicker — bottom sheet с перетаскиваемой полосой (ЗАФИКСИРОВАНО)

Обсуждали и утвердили в одной из прошлых сессий (устно, без записи — занесено
задним числом 2026-08-18 после того, как решение не нашлось в документах и
Вахтанг напомнил его заново).

Экран подбора материала — bottom sheet (выдвижная панель снизу) с двумя
зонами: сверху список "Отобрано" (уже добавленные позиции), снизу поиск +
drill-down по группам материалов. Между зонами — горизонтальная полоса-хендл,
перетаскиваемая пальцем, меняющая пропорцию между зонами.

**Диапазон хода полосы:** ограничен с двух сторон — верхняя зона может занимать
от 15% до 85% высоты экрана. То есть ни верхняя, ни нижняя зона никогда не
схлопывается полностью — у каждой всегда остаётся минимум 15% видимой высоты.

**Толщина хендла:** сделать заметно толще обычной тонкой разделительной линии
— чтобы полосу было легко подхватить пальцем на телефоне без промахов
(проблема, которую поймали раньше: тонкую полосу в крайних положениях сложно
захватить).

Прототип (см. переписку 2026-08-18) — драфт двух вариантов интерфейса
("A. Перетаскиваемый ползунок" вариант выбран, "B. Три фикс. режима" — не
выбран, отклонён/не обсуждался предметно). Именно вариант A с полосой и есть
то, что зафиксировано выше.

**Группы материалов на прототипе — НЕ реализовывать.** Уточнено 2026-08-18:
блок "Группы" (Автоматы/Кабель/Клеммники) на прототипе — заглушки для показа
общей идеи UI, в справочнике материалов категорий сейчас нет вообще. Для
первой версии MaterialPicker используется существующий плоский поиск по
материалам как есть (та же строка поиска, что уже в движке material-v2),
БЕЗ блока групп и БЕЗ drill-down. Древовидная иерархия материалов
(group→subgroup→material) — отдельная будущая задача, не смешивать с этим
шагом (см. "On the horizon" в основной памяти).

### 2.9 MaterialPicker — навигация и сохранение (ЗАФИКСИРОВАНО 2026-08-18, РЕАЛИЗОВАНО в v42)

**Место в UI:** MaterialPicker НЕ встроен в модалку состава kit, а открывается
как отдельный полноэкранный переход. Модалка просмотра состава kit (список
kit_item, названия+количество) остаётся как есть, но кнопка внизу
переименована из «+ Добавить» в **«Редактировать»** — временная форма
select+количество (простое добавление одной позиции, введена в v40 как
заглушка) полностью убирается, её заменяет MaterialPicker.

**Переход:** клик «Редактировать» в модалке kit → модалка закрывается →
открывается MaterialPicker на весь экран, работает поверх текущей страницы
(не отдельный URL — так же, как остальной UI движка не завязан на роутинг).

**Предзаполнение:** при открытии MaterialPicker раздел "Отобрано" сразу
подгружается текущим составом kit_item этого комплекта (не начинается
пустым) — человек видит и правит уже существующие позиции наравне с новыми.

**Черновик:** все изменения в "Отобрано" (добавление через поиск, +/-
количества, удаление позиции) — только в памяти экрана, kit_item на сервере
не трогаются до нажатия финальной кнопки.

**Финальная кнопка ("Добавить в калькуляцию" на прототипе — подпись будет
переформулирована под контекст kit, не про калькуляцию):** сохраняет черновик
ПОЛНОЙ ЗАМЕНОЙ существующего состава — на сервере все старые kit_item этого
kit удаляются и создаются заново из черновика, одной транзакцией. Не delta-
сравнение "что изменилось" — Вахтанг явно разрешил "смело переписывать всё"
ради простоты реализации.

**Кнопка «Отмена» / выход без сохранения:** обязательна. Черновик просто
отбрасывается, ничего на сервере не меняется.

**Возврат после Отмены/Сохранения (ИЗМЕНЕНО в v48, было иначе в v42-v47):**
и «Отмена», и «Сохранить состав» закрывают весь MaterialPicker и возвращают
СРАЗУ на экран со списком комплектов (drill-list) — БЕЗ промежуточного
повторного показа модалки просмотра состава kit. Раньше (v42-v47) обе кнопки
возвращали в модалку kit — оказалось лишним промежуточным шагом: человек
после сохранения/отмены всё равно почти всегда хотел просто уйти со всего
экрана редактирования комплекта, а не смотреть список состава ещё раз, и
модалку приходилось закрывать отдельным дополнительным тапом (см. прямую
просьбу Вахтанга: "окно там не нужно, его можно закрывать и сам не выходить
на экран с перечнем комплектов").

**Дубликаты материала в "Отобрано":** разрешены. Если один и тот же
material_id добавлен дважды (например, через повторный поиск) — это две
отдельные строки черновика с одинаковым material_id, НЕ суммируются
автоматически. При сохранении так и лягут как два отдельных kit_item.

**Порядок кодирования следующего шага:**
1. Переименовать кнопку модалки kit «+ Добавить» → «Редактировать», убрать
   старую простую форму select+количество
2. Реализовать MaterialPicker как отдельный fullscreen-компонент (bottom sheet
   с drag-полосой, поиск материалов — переиспользовать существующий плоский
   поиск, без групп)
3. Подключить: открытие с предзаполнением текущего состава, черновик в памяти,
   кнопки "Отмена"/финальная с полной заменой kit_item на сервере
4. Новый эндпоинт (или расширение существующего) для полной замены состава —
   уточнить при кодировании: отдельный `PUT /api/kit/{id}/items` (весь список
   разом) или переиспользование текущих create/delete kit_item по одной штуке
   в цикле — решить исходя из простоты на стороне бэкенда

---

## 3. ОТКРЫТЫЕ ВОПРОСЫ (разобрать в начале новой сессии, по 1-3 за раз)

### 3.1 Статус калькуляции
Вахтанг предложил три состояния: активна / подлежит архивированию /
помечена на удаление. Успели задать, но не получили ответ на:
- "Помечена на удаление" — это стандартный soft-delete проекта (is_deleted),
  или отдельное значение в поле статуса наряду с активна/архивная?
- Чем "подлежит архивированию" отличается по смыслу от soft-delete? Это
  разные состояния (архивная ещё видна/используется как справка, а
  удалённая — нет) или в целом близкие вещи, которые можно объединить?
- Нужно ли вообще формальное поле статуса сейчас, или (как решили раньше
  для другого аспекта) ввести только когда понадобится для логики
  "перепровести"? Вахтанг сам вернулся к статусу и явно попросил его
  проговорить — значит, видимо, нужен, но форма не определена.

### 3.2 Список допустимых брендов у заявки
Вопрос задан, ответ не получен:
- У заявки есть свой заранее заданный список допустимых брендов (тех, на
  которых считаем варианты именно в этой заявке), отдельный от общего
  списка брендов в системе?
- Или при копировании калькуляции в другую заявку выбор бренда — просто
  из полного общего списка брендов в системе, без ограничения per-заявка?

Это влияет на структуру request: если да — нужна связь request↔brand
(many-to-many или список), если нет — просто дропдаун из общего справочника
Brand при копировании calculation.

### 3.3 Правило генерации work_name (рабочего названия)
Вахтанг сказал "давай я подумаю", не финализировано. Известно:
- Точно нужны ДВА отдельных поля: client_name (как ввёл человек) и
  work_name (для базы/отображения).
- Пример-прикидка: префикс "Сборка" + клиентское название (пример:
  клиент написал "ШМУ" → рабочее "Сборка ШМУ").
- Более ранняя прикидка (может быть неактуальна): клиентское название +
  номер заказа + номер бренда.
- Не решено: генерируется ли work_name полностью автоматически и
  нередактируемо, или это подсказка, которую можно поправить вручную.
Нужно вернуться к этому вопросу отдельно, возможно на примерах реальных
заявок Вахтанга.

### 3.4 Поля kit_group и kit_section
Не договорили до конца — предварительно предложено "название + sort_order"
для обоих уровней (kit_section дополнительно — FK на kit_group), но нужно
подтвердить, не требуется ли что-то ещё (описание, код/шифр и т.п.) до того
как кодировать. Также не решено: нужны ли кнопки CRUD прямо в узле дерева
уже на первом шаге (kit_group/kit_section без kit), или сначала сделать
TreeList в режиме просмотра, а инлайн-редактирование добавить когда дерево
дойдёт до третьего уровня (kit).

### 3.9 Точный формат данных для TreeList
Компонент рендерит "плоский список узлов с id/parent_id/name/sort_order" —
нужно решить: это будет единый JSON-эндпоинт, отдающий все три уровня сразу
(group+section+kit одним запросом), или TreeList подгружает уровни лениво
по мере разворачивания узлов (запрос за подгруппами только при клике на
группу, запрос за комплектами только при клике на подгруппу)? Влияет на то,
сколько данных придётся тащить сразу при открытии экрана.

### 3.5 Точный список разделов комплектов
Упомянуты примеры (подключение силовых автоматов, подключение модульных
автоматов, установка силовых автоматов, установка модульных автоматов,
расширительные шины), но Вахтанг сам сказал "точные разделы нужно будет
проговорить" — список не финальный, нужно закрыть перед или во время
наполнения kit_section реальными данными.

### 3.6 Детали calculation_item (строки калькуляции)
Модель верхнего уровня есть ("материал напрямую" или "kit_id × quantity"),
но не спроектированы: точные поля строки, как хранится "тип строки"
(материал/комплект), нужен ли отдельный признак или просто nullable FK на
материал ИЛИ на kit (взаимоисключающе).

### 3.7 Детали request (заявка)
Только упомянута как будущая сущность-журнал, поля не обсуждались вообще
(клиент, дата, номер, статус заявки и т.д.) — предстоит спроектировать
отдельно, когда дойдёт очередь, но по архитектуре видно, что как минимум
понадобится поле(я) для списка допустимых брендов (см. 3.2) если ответ
на этот вопрос будет "да".

### 3.8 Спецификация и счёт (нижние звенья цепочки)
Вообще не обсуждались за пределами общего упоминания "калькуляция сводится
в спецификацию изделий, потом делается счёт". Как именно спецификация
агрегирует несколько calculation одной заявки, как считается итоговая
стоимость счёта — не спроектировано, будет актуально после того как
разберёмся с calculation и kit.

---

## 4. Рекомендуемый порядок действий в новой сессии

1. Прочитать этот файл целиком.
2. Прочитать docs/HANDOFF.md в репозитории — актуальное состояние кода
   (может быть уже другим, если что-то менялось параллельно).
3. Задать открытые вопросы 3.1 и 3.2 (они блокируют финализацию модели
   calculation) — по 1-3 за раз через ask_user_input_v0.
4. Как только 3.1–3.2 закрыты — зафиксировать финальную модель calculation
   в памяти (обновить/дополнить существующую запись памяти про калькуляцию).
5. Уточнить 3.4 (поля kit_section) и, если Вахтанг готов, 3.5 (список
   разделов) — и переходить к кодированию: kit_section как первый архив/шаг.
6. Вопросы 3.3, 3.6, 3.7, 3.8 — по мере приближения к соответствующим
   шагам, не обязательно все сразу в начале сессии.

Не забегать вперёд — после kit_section согласовать поля kit, затем
kit_items, затем возвращаться к calculation_item уже понимая, как реально
выглядит kit_id, на который он будет ссылаться.

---

## 5. Заявка → Калькуляция → Спецификация (сессия 2026-08-19, ЗАФИКСИРОВАНО)

Комплекты полностью готовы (v42). Это следующий крупный шаг. Ниже —
согласованная модель верхнего уровня, код ещё не писался. **Порядок
реализации снизу вверх: сначала zayavka, потом calculation, потом
specification.**

### 5.1 Порядок кодирования (решено)
1. `zayavka` — базовая таблица, CRUD-документ (без кнопок формирования
   спецификации на этом шаге)
2. `calculation` — с реальным (не заглушечным) FK `request_id` → zayavka,
   т.к. zayavka теперь делаем первой
3. `calculation_item` — материалы/комплекты, движок пересчёта
4. `specification` + `specification_item` — снимок по варианту, три кнопки
   формирования в zayavka

Ранее в этом файле (раздел 2.4) обсуждался вариант "временный nullable FK
или заявка-заглушка, пока zayavka не готова" — это снято, т.к. решили делать
zayavka первой. Раздел 3.7 ("детали request не обсуждались") — тоже снят,
детали ниже.

### 5.2 Таблица `zayavka` (заявка)
Поля (английские имена черновые, финально согласовать при кодировании):
- `client_id` (FK на клиента) — кто оформляет заявку
- `client_invoice_id` (FK на клиента, nullable, по умолчанию = client_id) —
  на кого выставляется счёт, если отличается от оформителя (частый кейс:
  один клиент заказывает, просит выставить счёт на другого)
- `brand_slot_1`, `brand_slot_2`, `brand_slot_3` — три справочных поля,
  "на каком бренде считать вариант N". Это ответ на прежний открытый
  вопрос 3.2 ("список допустимых брендов у заявки") — решение другое, чем
  предполагалось: не ограничение общего списка брендов, а прямое указание
  конкретного бренда на каждый из трёх слотов заявки.
- `is_deleted` — стандартный soft-delete проекта
- Копирование — server-side copy, как у kit (`POST /api/{key}/{id}/copy`)
- Физическое удаление — только отдельным обходчиком/чистильщиком, не через
  обычный UI (как решено ранее для kit)
- Три кнопки в UI заявки: "Сформировать спецификацию по варианту N"
  (N=1,2,3), независимые, без общей кнопки "пересчитать всё". При нажатии:
  актуализация цен калькуляций этого brand_slot + пересборка specification
  этого варианта (полная перезапись, delete-all+create-all — тот же паттерн,
  что и в kit_items).
- Дату/признак "когда последний раз актуализированы цены" — РЕШЕНО НЕ
  хранить (излишнее усложнение по прямому решению Вахтанга). Актуальность
  цен обеспечивается только тем, что пользователь сам нажимает кнопку
  формирования при необходимости.

### 5.3 Таблица `calculation` (калькуляция) — обновление раздела 2.4
Всё как в разделе 2.4, с изменениями:
- `request_id` — теперь РЕАЛЬНЫЙ FK на zayavka (не nullable-заглушка,
  т.к. zayavka кодируется первой)
- Группировка вариантов внутри заявки — через `brand_slot` (1/2/3),
  СОВПАДАЕТ по смыслу с `brand_slot_N` в zayavka. `brand_id` (FK на бренд)
  остаётся как справочное поле calculation, но НЕ является группирующим
  признаком — группировка через номер слота, не через сам бренд (уточнение
  по сравнению с более ранней записью в 2.5, где группировку связывали
  с `brand_id` напрямую; теперь это `brand_slot`, число 1/2/3).
- `product_name` — название изделия, вводит человек (соответствует
  `client_name` в старой записи 2.4, переименовано для ясности)
- `work_name` — рабочее название, генерируется по шаблону, см. 5.4
- `quantity`, статус (draft/active/archived_pending/delete_pending, без
  строгого порядка переходов) — как в старой записи userMemories

### 5.4 Правило генерации `work_name` (закрывает открытый вопрос 3.3)
- Текущее (актуальное) правило: `"Сборка " + product_name`
- Правило хранится НЕ жёстко в коде, а как настраиваемый текстовый шаблон
  в constants-таблице (key-value, уже есть в движке) — чтобы в будущем можно
  было добавить в шаблон номер заявки или код бренда без правки кода
- `work_name` — автоген с возможностью РУЧНОГО переопределения: если человек
  вручную поправил work_name, ставится флаг "переопределено", и авторасчёт
  больше не перезаписывает поле, пока флаг не сброшен (стандартный для
  проекта паттерн автоген+override)

### 5.5 Таблица `specification` + `specification_item` (снимок)
Назначение (важно, определяет всю структуру): спецификация — промежуточный
документ между калькуляцией и счётом-фактурой. Нужна потому что готовый
перечень изделий одного варианта не всегда переносится в счета 1-в-1: может
быть создано НЕСКОЛЬКО счетов-фактур из одной спецификации (например,
заказчик хочет оплатить сначала 3 изделия из 5, потом остальные 2) —
спецификация даёт стабильную базу для такой гибкой нарезки.

Из-за этого назначения — спецификация ЭТО СНИМОК, не live-ссылка (в отличие
от kit_items внутри calculation, которые именно live). Обоснование от
Вахтанга: если бы цена была live и кто-то пересчитал калькуляцию после
того как уже нарезали часть спецификации на выставленный счёт-фактуру,
существующий счёт-фактура задним числом "поехал" бы в цене — недопустимо
для уже выставленного финансового документа.

**`specification`** (документ-заголовок):
- `zayavka_id` (FK)
- `brand_slot` (1/2/3) — какой вариант

**`specification_item`** (строка):
- `specification_id` (FK)
- `calculation_id` (FK) — ссылка на источник, НЕ для live-подтяжки цены,
  а чтобы при повторном формировании можно было пройти по ссылкам и
  перечитать актуальные значения из calculation
- `product_name` — скопировано на момент формирования
- `price` — скопировано на момент формирования
- `quantity` — скопировано на момент формирования (уточнить при
  кодировании: берётся из calculation.quantity на момент снимка)

Механика формирования/обновления (по кнопке "Сформировать спецификацию по
варианту N" в zayavka): пересчитать все calculation этого (zayavka_id,
brand_slot) → для каждой сходить в актуальные name/price → полностью
переписать specification_item этого specification (delete-all+create-all,
тот же паттерн что и kit_items) → между нажатиями кнопки данные в
specification НЕ меняются сами по себе, чистый статичный снимок.

**Открытый вопрос (не решён, отложен до момента кодирования счёта):**
счёт-фактура при создании из спецификации — тоже копирует себе название/
цену/количество (снимок снимка, по той же логике "не должен поехать задним
числом"), или на этом уровне уже можно сослаться на specification_item
напрямую? Скорее всего по аналогии тоже снимок, но явно не подтверждено —
уточнить в начале работы над invoice.

### 5.6 Таблица `product` (журнал изделий для поиска, решено 2026-08-19)

Отвечает на вопрос "через два года нахожу изделие по названию + примерному
клиенту, где искать". Решение: НЕ отдельный сложный путь поиска по стадиям
документов, а один плоский справочник, устроенный по тому же принципу, что
`material` (обычная таблица через universal CRUD engine, с текстовым
поиском по полям).

**Механика связи с calculation (один-к-одному):**
- При сохранении `calculation` — автоматически создаётся (если ещё нет)
  или обновляется (если уже есть) соответствующая запись в `product`.
  Направление синхронизации: calculation ТОЛКАЕТ обновления в product при
  каждом своём сохранении/пересчёте — не наоборот (product не тянет живые
  данные из calculation при каждом обращении, как это устроено для
  specification/kit).
- `calculation.product_id` — FK на product, хранится на стороне calculation
  (не наоборот).
- Запись в product живёт, пока жива сама calculation. Что происходит с
  product при удалении calculation — ОТКРЫТЫЙ ВОПРОС, сознательно отложен
  ("отдельная процедура, подумаем что с этим дальше делать" — прямые слова
  Вахтанга). Не реализовывать удаление calculation, пока это не обсуждено
  отдельно.
- Запись в product может никогда не понадобиться для поиска — это ок,
  создаётся как задел "на всякий случай", не по требованию.

**Примерные поля `product` (не финализированы, уточнить при кодировании):**
- `name` / `work_name` — название изделия, ищется по подстроке
- `client_name` — клиент, ищется по подстроке
- `calculation_id` — обратная ссылка, чтобы от найденной записи провалиться
  в историю расчёта (заявка, бренд, спецификация и т.д.)
- `price` — актуальная цена на момент последнего сохранения calculation
- возможно `status`, если позже понадобится различать стадии
  (черновик/в работе/изготовлено/отгружено) прямо в этой таблице, а не
  только косвенно через статус calculation

**Соотношение с `finished_products` из общей иерархии документов:**
Изначально в общей схеме проекта (Request → Calculation → Brand
Specification → Invoice → Finished Products journal) предполагалась
отдельная таблица только для физически готовых/отгруженных изделий,
заполняемая на финальной стадии. По итогам этого обсуждения выбран другой
путь (вариант А из обсуждения): единая таблица `product`, живущая с самого
момента появления calculation, а не только на финальной стадии. Открыт
вопрос: остаётся ли отдельно узкое понятие "физически готово/отгружено"
как поле статуса внутри этой же таблицы product, или это всё ещё будет
отдельная будущая сущность поверх product — решить, когда дойдём до этой
части цепочки (после invoice).

Реализация `product` — НЕ первый шаг (порядок кодирования 5.1 не меняется:
zayavka → calculation → calculation_item → specification), а подключается
как побочный эффект сохранения calculation, когда до calculation дойдёт
очередь.

### 5.7 Что осталось открытым по этой теме
- Раздел 3.1 (статус калькуляции: soft-delete vs отдельные статусы vs both)
  — всё ещё не закрыт до конца, статус упомянут в 5.3 списком из старой
  записи памяти, но точная механика переходов между draft/active/
  archived_pending/delete_pending не проговорена
- Раздел 3.6 (точные поля calculation_item) — модель верхнего уровня
  подтверждена (два nullable FK: material_id/kit_id, тип по заполненному
  полю, две вкладки на фронте), но детальные поля не расписаны — сделать
  при кодировании шага 3 (после zayavka и calculation)
- Счёт-фактура (invoice) и журнал готовых изделий — не обсуждались вообще
  в этой сессии, будут актуальны после specification
