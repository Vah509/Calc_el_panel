# Handoff

## Состояние
Движок v25. Материалы и Бренды работают ТОЛЬКО через движок:
/material-v2, /brand-v2. Корень / редиректит на /material-v2.
Старые ручные реализации физически удалены (app/routers/ тоже —
подтверждено по логам последнего Repo Cleanup). Документация
движка — app/engine/ENGINE.md, сверена построчно с кодом, актуальна.

## Сделано в этой сессии
Сверка ENGINE.md с текущим кодом движка (config.py, formulas.py,
api.py, page.py, tables.py, register.py, page_router.py) — построчно,
без пропусков:
- Добавлен новый раздел ENGINE.md "Фильтр по связи (chips в
  filterbar)" — фича существовала в коде (page.py), но не была
  задокументирована: клик по чипу бренда в filterbar фильтрует
  список через тот же ?brand_id=... параметр, что и известное
  ограничение в api.py. Отдельная механика от чекбоксов поиска
  (enable_search_toggles) — работает всегда, если у таблицы есть
  relations, без отдельного флага
- register.py: обновлён устаревший комментарий в шапке файла —
  упоминал уже удалённые /materials-alpine, /brands-alpine
- Номер версии в шапке ENGINE.md синхронизирован с APP_VERSION (v25)

Остальной код движка (config.py, formulas.py, api.py, page.py,
tables.py, page_router.py) сверен с ENGINE.md — расхождений не
найдено, код и документ совпадают.

## Открыто
Ничего не открыто.
