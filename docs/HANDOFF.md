# Handoff

## Состояние
Движок v23. Меню приведено в порядок: два прямых пункта — Материалы
(/material-v2), Бренды (/brand-v2), без выпадающего списка и без
пометки "(движок)" — это теперь единственный вариант.

Старые страницы (/materials-alpine, /brands-alpine, /materials1) физически
ещё в коде и работают по прямому URL — просто убраны из навигации.
Их удаление из репозитория — отдельный следующий шаг, ещё не сделан.

## Сделано в этой сессии
- base.html: меню — 2 прямые ссылки вместо выпадающего списка на 5 пунктов
- style.css: добавлен .nav-link; .nav-dropdown-* классы больше не
  используются в разметке, но пока не удалены из CSS (будет вычищено
  вместе с остальным мусором на шаге очистки репозитория)

## Открыто
- Следующий шаг (по явной договорённости, отдельным действием): почистить
  репозиторий — удалить старые страницы/роуты материалов и брендов
  (materials-alpine, brands-alpine, materials1/sqladmin) и связанный
  мёртвый код/CSS, раз всё теперь работает через движок
