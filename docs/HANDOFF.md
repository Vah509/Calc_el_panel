# Handoff

## Состояние
Движок v22. Активные страницы: /material-v2, /brand-v2 (старые страницы —
/materials, /brands, /materials-alpine, /brands-alpine — рядом, не тронуты).
Последнее: поиск по артикулу у Материалов через отдельную галочку,
разметка галочек переехала в отдельную строку над строкой поиска.

## Сделано в этой сессии
- FieldConfig.search_default — состояние чекбокса поиска при первой
  загрузке страницы (артикул теперь toggleable, не always-on)
- material_table: по умолчанию активен только поиск по short_name;
  full_name и sku_article включаются вручную, выбор держится, пока
  страница открыта
- Вёрстка: галочки поиска — отдельная строка сверху (.search-toggles-row),
  строка поиска + фильтр по брендам — под ней, как раньше
- app/engine/ENGINE.md — обновлён раздел "Галочки поиска"

## Открыто
- (пусто)
