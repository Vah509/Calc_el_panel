# app/version.py
# ============================================================
# APP_VERSION и структура верхнего меню (base.html) — вынесены
# в отдельный модуль без зависимостей на остальное приложение,
# чтобы и main.py, и engine/register.py могли на него ссылаться
# без циклического импорта (main.py подключает register.py,
# который раньше пытался бы импортировать main.py обратно).
#
# Версия обновляется вручную при каждой значимой заливке архива —
# см. напоминание в конце ответа Claude при отправке нового шага.
# ============================================================

APP_VERSION = "v39.1"

# Верхнее меню — разделы с выпадающими пунктами. Каждый раздел:
# {"title": "...", "links": [{"label": "...", "url": "..."}, ...]}
# Поле называется "links", не "items" — в Jinja `section.items`
# резолвится в builtin-метод dict.items(), а не в наш ключ словаря,
# и ломает рендер (проверено на практике). Пустой links — раздел
# показывается, но выпадающий список пуст (для "Обработки", пока
# там нет ни одной готовой функции).
#
# "Группы комплектов" — единственный пункт для kit_group/kit_section:
# начиная с v38 это drill-down дерево на ОДНОЙ странице (kit_group
# открывается, kit_section виден внутри по клику, без перезагрузки) —
# отдельного пункта меню на kit_section больше нет, страница
# /kit_section-v2 существует только как заглушка на случай прямого
# перехода по старой ссылке (см. render_table_page).
#
# "Комплекты" / "Состав комплектов" — ВРЕМЕННЫЕ пункты (v39),
# обычные плоские страницы старого движка, НЕ подключены к дереву
# drill-down выше (см. app/models/kit.py, app/engine/tables.py) —
# способ вручную занести пробные записи до того, как будет
# спроектирован полноценный подбор материалов внутри карточки kit.
# Когда подбор будет готов, эти два пункта меню, скорее всего,
# исчезнут (kit станет доступен только изнутри дерева, как сейчас
# kit_section), а kit_items переедет внутрь карточки.
NAV_MENU = [
    {
        "title": "Справочники",
        "links": [
            {"label": "Материалы", "url": "/material-v2"},
            {"label": "Бренды", "url": "/brand-v2"},
            {"label": "Группы комплектов", "url": "/kit_group-v2"},
            {"label": "Комплекты (временный список)", "url": "/kit-v2"},
            {"label": "Состав комплектов (временный список)", "url": "/kit_item-v2"},
            {"label": "Константы", "url": "/constant-v2"},
        ],
    },
    {
        "title": "Обработки",
        "links": [
            {"label": "Пересчёты", "url": "/processors"},
        ],
    },
]
